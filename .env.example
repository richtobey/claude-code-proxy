# =============================================================================
# Claude Code Proxy — environment template (one file for all provider modes)
# Copy to .env and set the section that matches your chosen provider.
# =============================================================================

# -----------------------------------------------------------------------------
# API keys (set according to provider and fallback needs)
# -----------------------------------------------------------------------------
# Required for proxying to Anthropic (PREFERRED_PROVIDER=anthropic)
# ANTHROPIC_API_KEY="sk-ant-..."

# Required for OpenAI mapping or fallback (default provider)
OPENAI_API_KEY="sk-..."

# Required only if PREFERRED_PROVIDER=google and USE_VERTEX_AUTH=false
# Leave unset when using Vertex auth (USE_VERTEX_AUTH=true)
# GEMINI_API_KEY="your-google-ai-studio-key"

# -----------------------------------------------------------------------------
# Provider and model mapping
# -----------------------------------------------------------------------------
# One of: openai (default), google, anthropic
PREFERRED_PROVIDER="openai"

# Models to map haiku/sonnet to. Ignored when PREFERRED_PROVIDER=anthropic.
# Defaults for openai: gpt-4.1, gpt-4.1-mini
# Defaults for google (Gemini API): gemini-2.5-pro, gemini-2.5-flash
# For Vertex: use Vertex Model Garden IDs (Gemini or Claude — see below)
# BIG_MODEL="gpt-4.1"
# SMALL_MODEL="gpt-4.1-mini"

# -----------------------------------------------------------------------------
# OpenAI (optional overrides)
# -----------------------------------------------------------------------------
# OPENAI_BASE_URL="https://api.openai.com/v1"

# -----------------------------------------------------------------------------
# Google Vertex AI (only when PREFERRED_PROVIDER=google and USE_VERTEX_AUTH=true)
# Enables Gemini or Claude models via Vertex; no GEMINI_API_KEY needed.
#
# Authentication: use one of these (not both).
# - gcloud SDK: run "gcloud auth application-default login" and "gcloud config set project PROJECT_ID".
#   Leave VERTEX_CREDENTIALS_PATH unset; the proxy uses Application Default Credentials.
# - Service account key: set VERTEX_CREDENTIALS_PATH to the JSON key file path
#   (or set GOOGLE_APPLICATION_CREDENTIALS externally).
# -----------------------------------------------------------------------------
# USE_VERTEX_AUTH=true
# VERTEX_PROJECT="your-gcp-project-id"
# VERTEX_LOCATION="us-central1"
# Only if not using gcloud login (optional when using Application Default Credentials):
# VERTEX_CREDENTIALS_PATH="/absolute/path/to/service-account.json"

# -----------------------------------------------------------------------------
# Example presets (uncomment one block and set values)
# -----------------------------------------------------------------------------

# --- Preset: OpenAI (default) ---
# PREFERRED_PROVIDER="openai"
# BIG_MODEL="gpt-4.1"
# SMALL_MODEL="gpt-4.1-mini"

# --- Preset: Google via Gemini API key ---
# PREFERRED_PROVIDER="google"
# GEMINI_API_KEY="your-google-ai-studio-key"
# OPENAI_API_KEY="sk-..."  # fallback
# BIG_MODEL="gemini-2.5-pro"
# SMALL_MODEL="gemini-2.5-flash"

# --- Preset: Google Vertex (Gemini models) ---
# PREFERRED_PROVIDER="google"
# USE_VERTEX_AUTH=true
# VERTEX_PROJECT="your-gcp-project-id"
# VERTEX_LOCATION="us-central1"
# VERTEX_CREDENTIALS_PATH="/path/to/service-account.json"
# BIG_MODEL="gemini-2.5-pro"
# SMALL_MODEL="gemini-2.5-flash"

# --- Preset: Google Vertex (Claude models from Model Garden) ---
# PREFERRED_PROVIDER="google"
# USE_VERTEX_AUTH=true
# VERTEX_PROJECT="your-gcp-project-id"
# VERTEX_LOCATION="us-central1"
# VERTEX_CREDENTIALS_PATH="/path/to/service-account.json"
# BIG_MODEL="claude-sonnet-4-5@20250929"
# SMALL_MODEL="claude-haiku-4-5@20251001"

# --- Preset: Anthropic only (no remapping) ---
# ANTHROPIC_API_KEY="sk-ant-..."
# PREFERRED_PROVIDER="anthropic"
